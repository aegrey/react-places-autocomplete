import PlacesAutocomplete from './PlacesAutocomplete';
import {
  geocodeByAddress,
  geocodeByPlaceId,
  getLatLng,
  geocodeByCoordinates,
} from './utils';

export { geocodeByAddress, geocodeByPlaceId, getLatLng, geocodeByCoordinates };

export default PlacesAutocomplete;
